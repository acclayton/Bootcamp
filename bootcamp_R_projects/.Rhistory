Sys.time()
myTime <- function(){
Sys.time()
}
myTime()
x<-myTime()
y<-myTime()
y-x
start<-myTime()
runsum<-0
for(i in 1:100000){
runsum<-runsum+i
}
finish<-myTime()
ttime=finish-start
cat("The sum was ",runsum)
cat("it took ",ttime)
getName()
getName<- function(){
name<-readline(prompt="What is your name?")
cat("Hello ", name, " have a nice day!")
}
getName<- function(){
name<-readline(prompt="What is your name?")
cat("Hello ", name, " have a nice day!")
}
getName()
getName<- function(){
name<-readline(prompt="What is your name?")
cat("Hello", name, "have a nice day")
}
getName()
hunspell_check("blu")
install.packages("hunspell")
hunspell_check("blu")
library(hunspell)
hunspell_check("blu")
word<-readline("Type a word")
word<-readline("Check your spelling")
cat("Congratulations on spelling",word,"correctly")
spellcheck<-function(){
word<-readline("Type a word")
while (!hunspell_check(word)){
word<-readline("Check your spelling")
}
cat("Congratulations on spelling",word,"correctly")
}
spellcheck()
shoppinglist<-list()
append(shoppinglist, readline("What item do you want on your Shopping list?"))
shoppinglist
shoppinglist<-list()
shoppinglist<-append(shoppinglist, readline("What item do you want on your Shopping list?"))
shoppinglist
x<- readline("What item do you want on your Shopping list?")
shoppinglist<-append(shoppinglist,x)
x<-readline("What else do you want to buy? (type Quit to stop)")
shoppinglist<-list()
x<- readline("What item do you want on your Shopping list?")
shoppinglist<-append(shoppinglist,x)
x<-readline("What else do you want to buy? (type Quit to stop)")
s
View(shoppinglist)
shoppinglist<-list()
x<- readline("What item do you want on your Shopping list?")
shoppinglist<-append(shoppinglist,x)
x<-readline("What else do you want to buy? (type Quit to stop)")
s
d
f
1
2
3
x<- readline("What item do you want on your Shopping list?")
while(x!='Quit'){
shoppinglist<-append(shoppinglist,x)
x<-readline("What else do you want to buy? (type Quit to stop)")
}
shoppinglist
shoppinglist<-list()
x<- readline(prompt="What item do you want on your Shopping list?")
while(x!='Quit'){
shoppinglist<-append(shoppinglist,x)
x<-readline(prompt="What else do you want to buy? (type Quit to stop)")
}
shoppinglist
shoppinglist<-list()
x<- readline(prompt="What item do you want on your Shopping list?")
while(x!='Quit'){
shoppinglist<-append(shoppinglist,x)
x<-readline(prompt="What else do you want to buy? (type Quit to stop)")
}
shoppinglist
a<-readline(prompt="1")
c<-readline(prompt="1")
{
a<-readline(prompt="1")
b<-readline(prompt="1")
c<-readline(prompt="1")
}
shoppinglist<-list()
{
x<- readline(prompt="What item do you want on your Shopping list?")
}
while(x!='Quit'){
shoppinglist<-append(shoppinglist,x)
x<-readline(prompt="What else do you want to buy? (type Quit to stop)")
}
addItem<-function(){
temp<-readline(prompt="What item do you want on your Shopping list?")
return temp
}
{
shoppinglist<-list()
x<- addItem()
while(x!='Quit'){
shoppinglist<-append(shoppinglist,x)
x<-readline(prompt="What else do you want to buy? (type Quit to stop)")
}
}
{
shoppinglist<-list()
x<- readline(prompt="What item do you want on your Shopping list?")
while(x!='Quit'){
shoppinglist<-append(shoppinglist,x)
x<-readline(prompt="What else do you want to buy? (type Quit to stop)")
}
}
shoppinglist
for (x in shoppinglist){
print(x)
}
